apiVersion: v1
kind: LimitRange
metadata:
  name: amd-gpu-limits
  namespace: arc-runners
spec:
  limits:
    - default:
        amd.com/gpu: "1"
      defaultRequest:
        amd.com/gpu: "1"
      type: Container

